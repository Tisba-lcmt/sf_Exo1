{# Je créé un fichier base.html.twig, qui contient tous les éléments communs de mes pages (header, footer#}
<!DOCTYPE html>
<html lang="fr">
    {# En-tête du document #}
    <head>
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <meta name="description" content="Site d'informations qui publie des articles sur l'actualité">
        <meta name="author" content="Baptiste Lucmaret">
        <meta name="keywords" content="gouvernement, CBD, humain, électricité">
        <meta name="robots" content="index, follow">

        {# Lien vers mon fichier style.css en partant du dossier assets à l'aide de la methode asset()
        propre à twig et lié à Symfony. Cette méthode permet de viser. #}

        <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">

        {# Block qui a pour variable title, il sert à inclure le titre du document de mon site. #}
        <title>{% block title %}{% endblock title%}</title>

    </head>

    <body>

        {# En-tête du corps du document #}
        <header>

            {# Barre de navigation #}
            <nav class="row justify-around align-center w-100">

                <div class="logo">

                    <a href="{{ path('homepage') }}">
                        {# Lien vers mon fichier style.css en partant du dossier assets à l'aide de la methode asset()
                        propre à twig et lié à Symfony. #}
                        <img src="{{ asset('assets/img/Logo.png') }}" alt="Logo Le Monde" class="resp">
                    </a>

                </div>
                {# Je créé un lien avec la fonction path qui prend en paramètre le nom de la route. #}

                <li><a href="{{ path('homepage') }}">Accueil</a></li>
                <li><a href="{{ path('articles_list') }}">Articles</a></li>
                <li><a href="">Item 3</a></li>
                <li><a href="">Item 4</a></li>

            </nav>

        </header>

        {# Block qui a pour variable main, il sert à inclure le contenue de mes pages twig. #}
        {% block main %}{% endblock main %}

        {# Pied de page du corps du document. #}

        <footer class="relative" id="backgroundFooter">

            <div class="logo absolute">
                <img src="{{ asset('assets/img/logo.png') }}" alt="Logo Le Monde" class="resp">
            </div>

        </footer>

    </body>
</html>
